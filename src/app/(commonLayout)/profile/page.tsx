import SimpleLayout from '../../(communityLayout)/SimpleLayout';
import withLayout from '../../../layout/withLayout';
import Container from './ProfileContainer';

export default withLayout(Container, SimpleLayout);

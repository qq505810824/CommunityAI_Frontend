import withLayout from '../../../../layout/withLayout';
import SimpleLayout from '../../SimpleLayout';
import Container from './CourseCreateContainter';

export default withLayout(Container, SimpleLayout);
'use client';

import ChannelCreateView from './ChannelCreateView';

export default function ChannelCreateContainter() {
    return (
        <>
            <ChannelCreateView {...{}} />
        </>
    );
}
